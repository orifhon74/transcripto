flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

openai==1.40.0

# transcription
faster-whisper==1.0.3
# Fast diarization stack (CPU-friendly)
webrtcvad==2.0.10
resemblyzer==0.1.4
spectralcluster==0.2.4
pydub==0.25.1
httpx==0.27.2

# If you *might* use accurate diarization later (pyannote):
# torch CPU build (needed by resemblyzer anyway)
torch==2.2.2
# pyannote is optional; comment these out if you won't use it on Railway CPU
# pyannote.audio==3.1.1
# torchaudio==2.2.2
# soundfile==0.12.1
# huggingface_hub==0.24.6