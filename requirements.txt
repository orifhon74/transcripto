numpy==1.26.4

flask==3.0.3
gunicorn==22.0.0
python-dotenv==1.0.1

# OpenAI SDK
openai==1.40.0

# transcription
faster-whisper==1.0.3

# Fast diarization stack (CPU-friendly, fallback)
webrtcvad==2.0.10
resemblyzer==0.1.4
spectralcluster==0.2.4
pydub==0.25.1

# Fix for OpenAI SDK compatibility
httpx==0.27.2

# Torch CPU build (needed by resemblyzer anyway)
torch==2.2.2

# Optional accurate diarization (pyannote)
# Uncomment if you want to use pyannote on Railway
# (⚠ big and slow on CPU, but works if you set HUGGINGFACE_TOKEN)
pyannote.audio==3.1.1
torchaudio==2.2.2
soundfile==0.12.1
huggingface_hub==0.24.6